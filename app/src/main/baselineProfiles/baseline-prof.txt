# Baseline Profile: hand-curated (no macrobenchmark module in this project — see CLAUDE.md
# Testing section for why instrumentation tests aren't used here). Marks the app's own code for
# ahead-of-time compilation by ART at install time via androidx.profileinstaller, covering the
# cold-start path (MainActivity → MainScreen/MainViewModel → Room/repository → theme) instead of
# leaving it to interpret/JIT on first launch. Wildcard rules require AGP's baseline profile
# wildcard support (available since AGP 8.1; this project is on 9.2.1).
HSPLcom/freqcast/**;->**(**)**
Lcom/freqcast/**;
